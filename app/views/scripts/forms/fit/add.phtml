<h2>Add Fit</h2>
<form id="fit_add" method="post" class="form-horizontal well">
	<?php
		echo $form->renderCsrf();
		echo $form->renderRow('character_id', array('required' => 'required', 'class' => 'selectize'));
		echo $form->renderRow('data_string', array('required' => 'required'));
		echo $form->renderButtonRow('submit');
	?>
</form>